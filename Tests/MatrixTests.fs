module MatrixTests

open System
open MKLNET
open CsCheck

let ROWS = 100

let all =
    test "matrix" {
        test "add" {
            //let m = Matrix.Sqrt(null);
            //let _ = m * m;
            ()
        }
    }